Rebuild " docker-compose down "
Run " docker-compose up -d "
try username: admin, password: admin 
if not work:
    docker-compose exec airflow-webserver airflow users create --username admin --firstname Air --lastname Flow --role Admin --email admin@example.com --password admin


-- check SQL
docker exec -it final_proj-postgres-1 bash 
psql -U airflow -d airflow

-- SQL to check table
SELECT COUNT(*) FROM shopzada.dimDate;
SELECT COUNT(*) FROM shopzada.dimCampaign;
SELECT COUNT(*) FROM shopzada.dimMerchant;
SELECT COUNT(*) FROM shopzada.dimStaff;
SELECT COUNT(*) FROM shopzada.dimUser;
SELECT COUNT(*) FROM shopzada.dimProduct;



NOTES: (Cleaning - 11/30/25)
*fixed dupes for customer management
*fixed staff and merchant stuff 
*fixed staff and merchant duplicates (add key)
TO-DO NEXT:

UNCLEAN DATA:
> Business Department
   - product_list (yung toolss)
> Enterprise Department
   - order_with_merchant (cascade with keys)
> Marketing Department
   - transactional_campaign_data.csv
> Operations Department
   - all order files (cascade with keys)

FIX FOLDER STRUCTURE
- if possible, lipat load and cleaning scripts sa scripts/ lang and not organize by departments

LOAD DATA:
- finish scripts for Staging Layer
- finish scripts for Dimension Layer

DASHBOARD
- start the draft for DASHBOARD

DOCUMENTATION:
- Progress Report (12/1/25)
- Docs/Compilation of assumptions and rules we set on cleaning the data.





